server:
  port: 8080

spring:
  datasource:
    url: ${DB_URL}
    username: ${DB_USER}
    password: ${DB_PASSWORD}

app:
  security:
    jwt:
      secret: ${JWT_SECRET}
      access-exp-ms: 3600000
      refresh-exp-ms: ${JWT_REFRESH_EXP_MS:1209600000}